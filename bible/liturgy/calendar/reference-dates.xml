<?xml version="1.0" encoding="UTF-8"?>
<table xmlns="http://query.yahooapis.com/v1/schema/table.xsd">
  <meta>
    <author>Vic Mortelmans</author>
    <description>The table contains the a set of reference dates used for the liturgical calendar.</description>
    <sampleQuery>select * from bible.liturgy.calendar.reference-dates where year='2010'</sampleQuery>
    <documentationURL></documentationURL>
  </meta>
  <bindings>
    <select produces="XML" itemPath="">
      <urls>
        <url></url>
      </urls>
      <inputs>
        <key id="year" type="xs:string" paramType="variable" required="true"/>
      </inputs>
      <execute><![CDATA[ 

			]]></execute>
      </select>
  </bindings>
</table>
